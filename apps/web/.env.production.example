# Documentation template only. This file contains no deployable values.
# NEXT_PUBLIC_API_BASE_URL is embedded during the production image build.
NEXT_PUBLIC_API_BASE_URL=https://api.example.invalid

# Server-only, non-secret runtime routing configuration.
INTERNAL_API_BASE_URL=https://api.example.invalid
SESSION_COOKIE_NAME=amp_session
